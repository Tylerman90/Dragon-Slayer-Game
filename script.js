var slaying = true;

while (slaying) {
    slaying = false;
}

var youHit = function () {
    Math.floor(Math.random() * 2);
};

var damageThisRound = Math.floor(Math.random()*5 + 1);

var totalDamage = 0;